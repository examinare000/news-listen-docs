# Tech News Podcast App — iOS アプリ実装プラン

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** バックエンド API と連携して、RSS ニュース推薦フィード・Podcast 再生・設定管理ができる iOS アプリを SwiftUI で構築する。

**Architecture:** MVVM パターン。各タブ（Feed / Podcast / Settings）が ViewModel を持ち、`APIClient` を通じてバックエンドと通信する。音声再生は AVFoundation の `AVPlayer` を使用。`AppState` がユーザー設定（API URL・キー・難易度・再生速度）をローカル保持。

**Tech Stack:** Swift 5.10+, SwiftUI, Xcode 16, AVFoundation, URLSession (async/await), XCTest

**前提:** バックエンドデプロイが完了し、API URL と API キーが手元にあること。

---

## ファイル構成

> **注:** Xcode が生成するプロジェクトは `ios/NewsListenApp/`（プロジェクトコンテナ）直下に、ソース用 `NewsListenApp/` とテスト用 `NewsListenAppTests/` を並べる2階層構造になる。以下のパスは実態に合わせて記載する。

```
ios/NewsListenApp/                       # Xcode プロジェクトコンテナ（.xcodeproj を含む）
├── NewsListenApp.xcodeproj
├── NewsListenApp/                       # アプリソース（ターゲット: NewsListenApp）
│   ├── NewsListenAppApp.swift           # @main エントリポイント
│   ├── AppState.swift                   # 全体共有状態（UserDefaults ベース）
│   ├── Models/
│   │   ├── Article.swift                # Codable: フィード記事
│   │   ├── Podcast.swift                # Codable: Podcast エピソード
│   │   └── RssSource.swift              # Codable: RSS ソース設定
│   ├── Networking/
│   │   ├── APIClient.swift              # URLSession ベースの API クライアント
│   │   └── APIEndpoint.swift            # エンドポイント定義
│   ├── Feed/
│   │   ├── FeedView.swift               # フィードタブのルートビュー
│   │   ├── FeedViewModel.swift          # フィードデータ取得・スワイプ処理
│   │   └── ArticleRowView.swift         # 記事1件の行表示
│   ├── Podcast/
│   │   ├── PodcastView.swift            # Podcast タブのルートビュー
│   │   ├── PodcastViewModel.swift       # Podcast 一覧取得・再生管理
│   │   ├── PodcastRowView.swift         # Podcast1件の行表示
│   │   └── AudioPlayerView.swift        # 再生コントロール（速度・シークバー）
│   └── Settings/
│       ├── SettingsView.swift           # 設定タブのルートビュー
│       └── SettingsViewModel.swift      # RSS ソース管理・設定保存
└── NewsListenAppTests/                  # ユニットテスト（ターゲット: NewsListenAppTests）
    ├── APIClientTests.swift
    ├── FeedViewModelTests.swift
    ├── PodcastViewModelTests.swift
    └── SettingsViewModelTests.swift
```

---

## Task 1: Xcode プロジェクトセットアップ

**Files:** Xcode プロジェクト作成

- [x] **Step 1: Xcode で新規プロジェクトを作成**（完了）

Xcode を開き:
1. File → New → Project
2. iOS → App を選択
3. Product Name: `NewsListenApp`
4. Interface: `SwiftUI`
5. Language: `Swift`
6. Include Tests: ✅（XCTest）
7. 保存先: `ios/` ディレクトリ（実際は `ios/NewsListenApp/` 配下に生成された）

> **実施メモ:** Xcode のプロジェクト作成ウィザードで以下の点に留意する。
> - **Storage は `None` を選ぶ**: 初回作成時に SwiftData テンプレート（`Item.swift` と `ModelContainer`）が混入した。本アプリは SwiftData を使わないため、`Item.swift` を削除し `NewsListenAppApp.swift` から `ModelContainer` 関連コードを除去する（Task 4 で `AppState` ベースに置き換える）。
> - **「Create Git repository on my Mac」のチェックを外す**: チェックすると `ios/NewsListenApp/.git` が独立リポジトリとして作られ、本体リポジトリから追跡できなくなる。作成済みの場合は `rm -rf ios/NewsListenApp/.git` で除去してから Step 3 を行う。

- [x] **Step 2: ターゲット設定**（完了）

> **実施メモ:** App ターゲットは iOS 17.6（Xcode 既定）・Bundle ID は既定の `rioikeda.NewsListenApp`。生成時に Tests/UITests ターゲットの Deployment Target が iOS 26.4 になっていたため、本体に合わせ全6ビルド構成を iOS 17.6 に統一した。

Project Navigator でプロジェクトを選択:
- Deployment Target: iOS 17.0（Xcode 16 既定では 17.x になる。テスト/UIテストターゲットの Deployment Target も本体に合わせて統一すること）
- Bundle Identifier: 任意（生成時の既定は `rioikeda.NewsListenApp`）

- [x] **Step 3: ネストした git を除去し、本体リポジトリへ追加**（完了）

> **実施メモ:** ネストした `.git` は存在しなかった。SwiftData テンプレート（`Item.swift`・`ModelContainer`）を除去し、`ContentView` はセットアップ用プレースホルダに置き換えてからコミットした。なお当環境は Command Line Tools のみで Xcode 本体が無いため、ビルド/テスト（⌘B/⌘U）は Xcode 上で実施が必要。

```bash
# Xcode が作った独立リポジトリを除去（作成済みの場合のみ）
rm -rf ios/NewsListenApp/.git
git add ios/
git commit -m "iOSアプリのXcodeプロジェクト初期化"
```

---

## Task 2: データモデル

**Files:**
- Create: `ios/NewsListenApp/NewsListenApp/Models/Article.swift`
- Create: `ios/NewsListenApp/NewsListenApp/Models/Podcast.swift`
- Create: `ios/NewsListenApp/NewsListenApp/Models/RssSource.swift`

- [x] **Step 1: テストを書く（RED）**

`ios/NewsListenApp/NewsListenAppTests/ModelTests.swift` を作成:

```swift
import XCTest
@testable import NewsListenApp

final class ModelTests: XCTestCase {

    func testArticleDecodesFromJSON() throws {
        let json = """
        {
            "id": "abc123",
            "title": "Rust is amazing",
            "url": "https://example.com/rust",
            "source": "hackernews",
            "score": 0.9,
            "published_at": "2026-05-31T06:00:00Z"
        }
        """.data(using: .utf8)!

        let article = try JSONDecoder().decode(Article.self, from: json)
        XCTAssertEqual(article.id, "abc123")
        XCTAssertEqual(article.title, "Rust is amazing")
        XCTAssertEqual(article.score, 0.9)
    }

    func testPodcastDecodesFromJSON() throws {
        let json = """
        {
            "id": "pod1",
            "type": "single",
            "article_ids": ["abc123"],
            "difficulty": "toeic_900",
            "audio_url": "https://storage.example.com/pod1.mp3",
            "japanese_intro_text": "今日のニュースは...",
            "duration_seconds": 300,
            "created_at": "2026-05-31T06:00:00Z"
        }
        """.data(using: .utf8)!

        let podcast = try JSONDecoder().decode(Podcast.self, from: json)
        XCTAssertEqual(podcast.id, "pod1")
        XCTAssertEqual(podcast.durationSeconds, 300)
        XCTAssertEqual(podcast.difficulty, "toeic_900")
    }

    func testFeedResponseDecodes() throws {
        let json = """
        {
            "articles": [],
            "date": "2026-05-31"
        }
        """.data(using: .utf8)!

        let feed = try JSONDecoder().decode(FeedResponse.self, from: json)
        XCTAssertEqual(feed.date, "2026-05-31")
        XCTAssertTrue(feed.articles.isEmpty)
    }
}
```

Xcode でテストを実行（⌘U）して失敗することを確認（RED）。

- [x] **Step 2: Article.swift を作成（GREEN）**

```swift
// ios/NewsListenApp/NewsListenApp/Models/Article.swift
import Foundation

struct Article: Codable, Identifiable {
    let id: String
    let title: String
    let url: String
    let source: String
    let score: Double
    let publishedAt: String

    enum CodingKeys: String, CodingKey {
        case id, title, url, source, score
        case publishedAt = "published_at"
    }
}

struct FeedResponse: Codable {
    let articles: [Article]
    let date: String
}
```

- [x] **Step 3: Podcast.swift を作成**

```swift
// ios/NewsListenApp/NewsListenApp/Models/Podcast.swift
import Foundation

struct Podcast: Codable, Identifiable {
    let id: String
    let type: String
    let articleIds: [String]
    let difficulty: String
    let audioUrl: String
    let japaneseIntroText: String
    let durationSeconds: Int
    let createdAt: String

    enum CodingKeys: String, CodingKey {
        case id, type, difficulty
        case articleIds = "article_ids"
        case audioUrl = "audio_url"
        case japaneseIntroText = "japanese_intro_text"
        case durationSeconds = "duration_seconds"
        case createdAt = "created_at"
    }

    var formattedDuration: String {
        let minutes = durationSeconds / 60
        let seconds = durationSeconds % 60
        return String(format: "%d:%02d", minutes, seconds)
    }
}

struct PodcastListResponse: Codable {
    let podcasts: [Podcast]
}
```

- [x] **Step 4: RssSource.swift を作成**

```swift
// ios/NewsListenApp/NewsListenApp/Models/RssSource.swift
import Foundation

struct RssSource: Codable, Identifiable {
    let name: String
    let url: String
    var id: String { url }
}

struct RssSourcesResponse: Codable {
    let sources: [RssSource]
}
```

- [x] **Step 5: テストを実行してパスを確認（GREEN）**

Xcode でテストを実行（⌘U）。`ModelTests` が全て通ることを確認。

- [x] **Step 6: コミット**

```bash
git add ios/NewsListenApp/NewsListenApp/Models/ ios/NewsListenApp/NewsListenAppTests/ModelTests.swift
git commit -m "iOSアプリのデータモデル（Article/Podcast/RssSource）を定義"
```

---

## Task 3: API クライアント

**Files:**
- Create: `ios/NewsListenApp/NewsListenApp/Networking/APIEndpoint.swift`
- Create: `ios/NewsListenApp/NewsListenApp/Networking/APIClient.swift`

- [x] **Step 1: テストを書く（RED）**

`ios/NewsListenApp/NewsListenAppTests/APIClientTests.swift` を作成:

```swift
import XCTest
@testable import NewsListenApp

final class APIClientTests: XCTestCase {

    func testFetchFeedDecodesResponse() async throws {
        let mockJSON = """
        {"articles": [{"id":"a1","title":"Test","url":"https://example.com","source":"hackernews","score":0.8,"published_at":"2026-05-31T06:00:00Z"}], "date": "2026-05-31"}
        """.data(using: .utf8)!

        let client = APIClient(
            baseURL: URL(string: "https://api.example.com")!,
            apiKey: "test-key",
            session: MockURLSession(data: mockJSON, statusCode: 200)
        )

        let feed = try await client.fetchFeed()
        XCTAssertEqual(feed.articles.count, 1)
        XCTAssertEqual(feed.articles[0].id, "a1")
    }

    func testStarArticleCallsCorrectEndpoint() async throws {
        let mockJSON = """{"status":"starred","article_id":"a1"}""".data(using: .utf8)!
        let mockSession = MockURLSession(data: mockJSON, statusCode: 200)

        let client = APIClient(
            baseURL: URL(string: "https://api.example.com")!,
            apiKey: "test-key",
            session: mockSession
        )

        try await client.starArticle(id: "a1")

        XCTAssertEqual(mockSession.lastRequest?.url?.path, "/articles/a1/star")
        XCTAssertEqual(mockSession.lastRequest?.httpMethod, "POST")
    }

    func testAPIKeyIsIncludedInHeader() async throws {
        let mockJSON = """{"articles":[],"date":"2026-05-31"}""".data(using: .utf8)!
        let mockSession = MockURLSession(data: mockJSON, statusCode: 200)

        let client = APIClient(
            baseURL: URL(string: "https://api.example.com")!,
            apiKey: "secret-key",
            session: mockSession
        )

        _ = try await client.fetchFeed()
        XCTAssertEqual(mockSession.lastRequest?.value(forHTTPHeaderField: "X-API-Key"), "secret-key")
    }
}

// MARK: - MockURLSession

class MockURLSession: URLSessionProtocol {
    let data: Data
    let statusCode: Int
    var lastRequest: URLRequest?

    init(data: Data, statusCode: Int) {
        self.data = data
        self.statusCode = statusCode
    }

    func data(for request: URLRequest) async throws -> (Data, URLResponse) {
        lastRequest = request
        let response = HTTPURLResponse(
            url: request.url!,
            statusCode: statusCode,
            httpVersion: nil,
            headerFields: nil
        )!
        return (data, response)
    }
}
```

- [x] **Step 2: APIEndpoint.swift を作成**

```swift
// ios/NewsListenApp/NewsListenApp/Networking/APIEndpoint.swift
import Foundation

enum APIEndpoint {
    case feed
    case starArticle(id: String)
    case dismissArticle(id: String)
    case podcasts
    case sources
    case addSource
    case removeSource(url: String)

    var path: String {
        switch self {
        case .feed: return "/feed"
        case .starArticle(let id): return "/articles/\(id)/star"
        case .dismissArticle(let id): return "/articles/\(id)/dismiss"
        case .podcasts: return "/podcasts"
        case .sources, .addSource: return "/settings/sources"
        case .removeSource: return "/settings/sources"
        }
    }

    var method: String {
        switch self {
        case .feed, .podcasts, .sources: return "GET"
        case .starArticle, .dismissArticle, .addSource: return "POST"
        case .removeSource: return "DELETE"
        }
    }
}
```

- [x] **Step 3: URLSessionProtocol を定義して APIClient.swift を作成**

```swift
// ios/NewsListenApp/NewsListenApp/Networking/APIClient.swift
import Foundation

protocol URLSessionProtocol {
    func data(for request: URLRequest) async throws -> (Data, URLResponse)
}

extension URLSession: URLSessionProtocol {}

enum APIError: LocalizedError {
    case invalidURL
    case httpError(statusCode: Int)
    case decodingError(Error)

    var errorDescription: String? {
        switch self {
        case .invalidURL: return "Invalid URL"
        case .httpError(let code): return "HTTP Error \(code)"
        case .decodingError(let e): return "Decoding error: \(e.localizedDescription)"
        }
    }
}

@MainActor
final class APIClient: ObservableObject {
    private let baseURL: URL
    private let apiKey: String
    private let session: URLSessionProtocol
    private let decoder: JSONDecoder

    init(baseURL: URL, apiKey: String, session: URLSessionProtocol = URLSession.shared) {
        self.baseURL = baseURL
        self.apiKey = apiKey
        self.session = session
        self.decoder = JSONDecoder()
    }

    // MARK: - Feed

    func fetchFeed() async throws -> FeedResponse {
        try await request(.feed, responseType: FeedResponse.self)
    }

    func starArticle(id: String) async throws {
        try await requestVoid(.starArticle(id: id))
    }

    func dismissArticle(id: String) async throws {
        try await requestVoid(.dismissArticle(id: id))
    }

    // MARK: - Podcasts

    func fetchPodcasts() async throws -> PodcastListResponse {
        try await request(.podcasts, responseType: PodcastListResponse.self)
    }

    // MARK: - Settings

    func fetchSources() async throws -> RssSourcesResponse {
        try await request(.sources, responseType: RssSourcesResponse.self)
    }

    func addSource(name: String, url: String) async throws -> RssSourcesResponse {
        let body = ["name": name, "url": url]
        return try await request(.addSource, body: body, responseType: RssSourcesResponse.self)
    }

    func removeSource(url: String) async throws {
        var components = URLComponents(url: baseURL.appendingPathComponent(APIEndpoint.removeSource(url: url).path), resolvingAgainstBaseURL: false)!
        components.queryItems = [URLQueryItem(name: "url", value: url)]
        var req = URLRequest(url: components.url!)
        req.httpMethod = "DELETE"
        req.setValue(apiKey, forHTTPHeaderField: "X-API-Key")
        let (_, response) = try await session.data(for: req)
        try validateResponse(response)
    }

    // MARK: - Private helpers

    private func request<T: Decodable>(
        _ endpoint: APIEndpoint,
        body: [String: Any]? = nil,
        responseType: T.Type
    ) async throws -> T {
        let req = try buildRequest(endpoint, body: body)
        let (data, response) = try await session.data(for: req)
        try validateResponse(response)
        do {
            return try decoder.decode(T.self, from: data)
        } catch {
            throw APIError.decodingError(error)
        }
    }

    private func requestVoid(_ endpoint: APIEndpoint, body: [String: Any]? = nil) async throws {
        let req = try buildRequest(endpoint, body: body)
        let (_, response) = try await session.data(for: req)
        try validateResponse(response)
    }

    private func buildRequest(_ endpoint: APIEndpoint, body: [String: Any]?) throws -> URLRequest {
        let url = baseURL.appendingPathComponent(endpoint.path)
        var request = URLRequest(url: url)
        request.httpMethod = endpoint.method
        request.setValue(apiKey, forHTTPHeaderField: "X-API-Key")
        if let body {
            request.setValue("application/json", forHTTPHeaderField: "Content-Type")
            request.httpBody = try JSONSerialization.data(withJSONObject: body)
        }
        return request
    }

    private func validateResponse(_ response: URLResponse) throws {
        guard let http = response as? HTTPURLResponse else { return }
        guard 200..<300 ~= http.statusCode else {
            throw APIError.httpError(statusCode: http.statusCode)
        }
    }
}
```

- [x] **Step 4: テストを実行してパスを確認（GREEN）**

Xcode でテストを実行（⌘U）。`APIClientTests` が全て通ることを確認。

- [x] **Step 5: コミット**

```bash
git add ios/NewsListenApp/NewsListenApp/Networking/ ios/NewsListenApp/NewsListenAppTests/APIClientTests.swift
git commit -m "URLSessionベースのAPIクライアントを実装（feed/star/dismiss/podcasts/settings）"
```

---

## Task 4: AppState と エントリポイント

> **実施メモ:** 現在のプロジェクトは nonisolated 既定 + `@MainActor` な `APIClient` のため、`apiClient` を生成する `AppState` も `@MainActor` を付与した。また `ContentView` が参照する `FeedView`/`PodcastView`/`SettingsView` は Task 5〜7 で実装するため、各タスクでビルドを通す目的で正規パスに最小プレースホルダを先行作成し、後続タスクで中身を差し替える。旧 `ContentView.swift`（セットアップ用プレースホルダ）は `ContentView` を本ファイルへ移したため削除した。

**Files:**
- Create: `ios/NewsListenApp/NewsListenApp/AppState.swift`
- Modify: `ios/NewsListenApp/NewsListenApp/NewsListenAppApp.swift`

- [x] **Step 1: AppState.swift を作成**

```swift
// ios/NewsListenApp/NewsListenApp/AppState.swift
import Foundation
import Combine

final class AppState: ObservableObject {
    // UserDefaults キー
    private enum Keys {
        static let apiBaseURL = "api_base_url"
        static let apiKey = "api_key"
        static let defaultDifficulty = "default_difficulty"
        static let defaultPlaybackSpeed = "default_playback_speed"
    }

    @Published var apiBaseURL: String {
        didSet { UserDefaults.standard.set(apiBaseURL, forKey: Keys.apiBaseURL) }
    }

    @Published var apiKey: String {
        didSet { UserDefaults.standard.set(apiKey, forKey: Keys.apiKey) }
    }

    @Published var defaultDifficulty: String {
        didSet { UserDefaults.standard.set(defaultDifficulty, forKey: Keys.defaultDifficulty) }
    }

    @Published var defaultPlaybackSpeed: Double {
        didSet { UserDefaults.standard.set(defaultPlaybackSpeed, forKey: Keys.defaultPlaybackSpeed) }
    }

    var apiClient: APIClient? {
        guard !apiBaseURL.isEmpty, !apiKey.isEmpty,
              let url = URL(string: apiBaseURL) else { return nil }
        return APIClient(baseURL: url, apiKey: apiKey)
    }

    var isConfigured: Bool { !apiBaseURL.isEmpty && !apiKey.isEmpty }

    init() {
        self.apiBaseURL = UserDefaults.standard.string(forKey: Keys.apiBaseURL) ?? ""
        self.apiKey = UserDefaults.standard.string(forKey: Keys.apiKey) ?? ""
        self.defaultDifficulty = UserDefaults.standard.string(forKey: Keys.defaultDifficulty) ?? "toeic_900"
        self.defaultPlaybackSpeed = UserDefaults.standard.double(forKey: Keys.defaultPlaybackSpeed).nonZero ?? 1.0
    }
}

extension Double {
    var nonZero: Double? { self == 0 ? nil : self }
}
```

- [x] **Step 2: NewsListenAppApp.swift を更新**

```swift
// ios/NewsListenApp/NewsListenApp/NewsListenAppApp.swift
import SwiftUI

@main
struct NewsListenAppApp: App {
    @StateObject private var appState = AppState()

    var body: some Scene {
        WindowGroup {
            if appState.isConfigured {
                ContentView()
                    .environmentObject(appState)
            } else {
                InitialSetupView()
                    .environmentObject(appState)
            }
        }
    }
}

// 初回設定画面（API URL と API キーを入力）
struct InitialSetupView: View {
    @EnvironmentObject var appState: AppState
    @State private var urlInput = ""
    @State private var keyInput = ""

    var body: some View {
        NavigationStack {
            Form {
                Section("API 設定") {
                    TextField("API URL (例: https://podcast-api-xxx.run.app)", text: $urlInput)
                        .autocorrectionDisabled()
                        .textInputAutocapitalization(.never)
                    SecureField("API キー", text: $keyInput)
                }
                Button("設定を保存") {
                    appState.apiBaseURL = urlInput
                    appState.apiKey = keyInput
                }
                .disabled(urlInput.isEmpty || keyInput.isEmpty)
            }
            .navigationTitle("初期設定")
        }
    }
}

// メインタブビュー
struct ContentView: View {
    var body: some View {
        TabView {
            FeedView()
                .tabItem { Label("フィード", systemImage: "newspaper") }
            PodcastView()
                .tabItem { Label("Podcast", systemImage: "headphones") }
            SettingsView()
                .tabItem { Label("設定", systemImage: "gearshape") }
        }
    }
}
```

- [x] **Step 3: コミット**

```bash
git add ios/NewsListenApp/NewsListenApp/AppState.swift ios/NewsListenApp/NewsListenApp/NewsListenAppApp.swift
git commit -m "AppState（UserDefaults永続化）と初期設定画面・タブ構造を実装"
```

---

## Task 5: Feed タブ

> **実施メモ（要件・設計とのすり合わせ後の方針）:** 以下のコードスニペットは初版。実装は次の通り是正する。
> 1. **FeedView の DI 修正**: プレースホルダ APIClient + `task` 内別 VM 生成の方式（StateObject 再設定不可問題）は採らず、`FeedView(apiClient:)` を `ContentView` から注入し `@StateObject` を `init` で生成する。`ContentView` は `appState.apiClient` を guard して渡す。
> 2. **記事タップは設定連動**（要件 §3.2 / AC-5・設計 §7）: `AppState.articleOpenMode`（`in_app`/`external`、既定 `in_app`）に従い、`in_app` は `SafariView`（`SFSafariViewController`）を `.sheet` 表示、`external` は `openURL` で外部起動。`UIApplication.shared.open` 直書きはしない。
> 3. **追加ファイル**: `Feed/SafariView.swift`（`UIViewControllerRepresentable`）。`ArticleRowView` はスコアを横バーで可視化（設計 §7）。`Settings` に「記事の開き方」`Picker` を最小実装（本実装は Task 7）。

**Files:**
- Create: `ios/NewsListenApp/NewsListenApp/Feed/FeedViewModel.swift`
- Create: `ios/NewsListenApp/NewsListenApp/Feed/ArticleRowView.swift`
- Create: `ios/NewsListenApp/NewsListenApp/Feed/SafariView.swift`
- Create: `ios/NewsListenApp/NewsListenApp/Feed/FeedView.swift`
- Create: `ios/NewsListenApp/NewsListenAppTests/FeedViewModelTests.swift`
- Modify: `ios/NewsListenApp/NewsListenApp/AppState.swift`（`articleOpenMode` 追加）
- Modify: `ios/NewsListenApp/NewsListenApp/NewsListenAppApp.swift`（`ContentView` で `FeedView(apiClient:)` 配線）
- Modify: `ios/NewsListenApp/NewsListenApp/Settings/SettingsView.swift`（記事の開き方トグル）

- [x] **Step 1: FeedViewModel のテストを書く（RED）**

```swift
// ios/NewsListenApp/NewsListenAppTests/FeedViewModelTests.swift
import XCTest
@testable import NewsListenApp

@MainActor
final class FeedViewModelTests: XCTestCase {

    func testLoadFeedPopulatesArticles() async throws {
        let mockJSON = """
        {"articles": [
            {"id":"a1","title":"Rust","url":"https://example.com","source":"hackernews","score":0.9,"published_at":"2026-05-31T06:00:00Z"},
            {"id":"a2","title":"Go","url":"https://example.com/go","source":"zenn","score":0.7,"published_at":"2026-05-31T05:00:00Z"}
        ], "date": "2026-05-31"}
        """.data(using: .utf8)!

        let client = APIClient(
            baseURL: URL(string: "https://api.example.com")!,
            apiKey: "key",
            session: MockURLSession(data: mockJSON, statusCode: 200)
        )
        let vm = FeedViewModel(apiClient: client)

        await vm.loadFeed()

        XCTAssertEqual(vm.articles.count, 2)
        XCTAssertFalse(vm.isLoading)
        XCTAssertNil(vm.errorMessage)
    }

    func testStarArticleRemovesItFromFeed() async throws {
        let starJSON = """{"status":"starred","article_id":"a1"}""".data(using: .utf8)!
        let client = APIClient(
            baseURL: URL(string: "https://api.example.com")!,
            apiKey: "key",
            session: MockURLSession(data: starJSON, statusCode: 200)
        )
        let vm = FeedViewModel(apiClient: client)
        let article = Article(id: "a1", title: "Test", url: "https://example.com", source: "hackernews", score: 0.9, publishedAt: "2026-05-31T06:00:00Z")
        vm.articles = [article]

        await vm.star(article: article)

        XCTAssertTrue(vm.articles.isEmpty)
    }

    func testDismissArticleRemovesItFromFeed() async throws {
        let dismissJSON = """{"status":"dismissed","article_id":"a1"}""".data(using: .utf8)!
        let client = APIClient(
            baseURL: URL(string: "https://api.example.com")!,
            apiKey: "key",
            session: MockURLSession(data: dismissJSON, statusCode: 200)
        )
        let vm = FeedViewModel(apiClient: client)
        let article = Article(id: "a1", title: "Test", url: "https://example.com", source: "hackernews", score: 0.9, publishedAt: "2026-05-31T06:00:00Z")
        vm.articles = [article]

        await vm.dismiss(article: article)

        XCTAssertTrue(vm.articles.isEmpty)
    }
}
```

- [x] **Step 2: FeedViewModel.swift を作成（GREEN）**

```swift
// ios/NewsListenApp/NewsListenApp/Feed/FeedViewModel.swift
import Foundation

@MainActor
final class FeedViewModel: ObservableObject {
    @Published var articles: [Article] = []
    @Published var isLoading = false
    @Published var errorMessage: String?

    private let apiClient: APIClient

    init(apiClient: APIClient) {
        self.apiClient = apiClient
    }

    func loadFeed() async {
        isLoading = true
        errorMessage = nil
        do {
            let response = try await apiClient.fetchFeed()
            articles = response.articles
        } catch {
            errorMessage = error.localizedDescription
        }
        isLoading = false
    }

    func star(article: Article) async {
        do {
            try await apiClient.starArticle(id: article.id)
            articles.removeAll { $0.id == article.id }
        } catch {
            errorMessage = error.localizedDescription
        }
    }

    func dismiss(article: Article) async {
        do {
            try await apiClient.dismissArticle(id: article.id)
            articles.removeAll { $0.id == article.id }
        } catch {
            errorMessage = error.localizedDescription
        }
    }
}
```

- [x] **Step 3: ArticleRowView.swift を作成**

```swift
// ios/NewsListenApp/NewsListenApp/Feed/ArticleRowView.swift
import SwiftUI

struct ArticleRowView: View {
    let article: Article

    var body: some View {
        VStack(alignment: .leading, spacing: 6) {
            Text(article.title)
                .font(.headline)
                .lineLimit(3)
            HStack {
                Text(article.source)
                    .font(.caption)
                    .foregroundStyle(.secondary)
                Spacer()
                Text(article.publishedAt.prefix(10))
                    .font(.caption)
                    .foregroundStyle(.secondary)
            }
        }
        .padding(.vertical, 4)
    }
}
```

- [x] **Step 4: FeedView.swift を作成**

```swift
// ios/NewsListenApp/NewsListenApp/Feed/FeedView.swift
import SwiftUI

struct FeedView: View {
    @EnvironmentObject var appState: AppState
    @StateObject private var viewModel: FeedViewModel

    init() {
        // AppState はビューが表示される時に注入するため、
        // ここでは仮の初期化。onAppear で apiClient を設定する。
        _viewModel = StateObject(wrappedValue: FeedViewModel(
            apiClient: APIClient(baseURL: URL(string: "https://placeholder.example.com")!, apiKey: "")
        ))
    }

    var body: some View {
        NavigationStack {
            Group {
                if viewModel.isLoading {
                    ProgressView("読み込み中...")
                } else if viewModel.articles.isEmpty {
                    ContentUnavailableView(
                        "記事がありません",
                        systemImage: "newspaper",
                        description: Text("しばらく後に再度確認してください")
                    )
                } else {
                    articleList
                }
            }
            .navigationTitle("フィード")
            .toolbar {
                Button(action: { Task { await reload() } }) {
                    Image(systemName: "arrow.clockwise")
                }
            }
            .alert("エラー", isPresented: .constant(viewModel.errorMessage != nil)) {
                Button("OK") { viewModel.errorMessage = nil }
            } message: {
                Text(viewModel.errorMessage ?? "")
            }
        }
        .task { await reload() }
    }

    private var articleList: some View {
        List(viewModel.articles) { article in
            ArticleRowView(article: article)
                .swipeActions(edge: .trailing, allowsFullSwipe: true) {
                    // 左スワイプ → Dismiss（赤）
                    Button(role: .destructive) {
                        Task { await viewModel.dismiss(article: article) }
                    } label: {
                        Label("Dismiss", systemImage: "xmark")
                    }
                }
                .swipeActions(edge: .leading, allowsFullSwipe: true) {
                    // 右スワイプ → Star（黄）
                    Button {
                        Task { await viewModel.star(article: article) }
                    } label: {
                        Label("Star", systemImage: "star.fill")
                    }
                    .tint(.yellow)
                }
                .onTapGesture {
                    if let url = URL(string: article.url) {
                        UIApplication.shared.open(url)
                    }
                }
        }
    }

    private func reload() async {
        guard let client = appState.apiClient else { return }
        let vm = FeedViewModel(apiClient: client)
        // StateObject を再設定できないため ViewModel を別途管理
        await viewModel.loadFeed()
    }
}
```

> **注意:** `FeedView` の `viewModel` は `@StateObject` で保持するため、`apiClient` は `task` モディファイアで `appState.apiClient` を参照して `loadFeed()` を呼ぶ設計にする。`FeedViewModel` のイニシャライザに `appState.apiClient` を渡す場合は `FeedView` の `init` を `@EnvironmentObject` 対応に変更する必要がある（Xcode 16 以降は `@Environment(AppState.self)` で対応）。

- [x] **Step 5: テストを実行してパスを確認**

Xcode でテストを実行（⌘U）。`FeedViewModelTests` が全て通ることを確認。

- [x] **Step 6: コミット**

```bash
git add ios/NewsListenApp/NewsListenApp/Feed/ ios/NewsListenApp/NewsListenAppTests/FeedViewModelTests.swift
git commit -m "Feedタブ（記事一覧・右スワイプStar・左スワイプDismiss）を実装"
```

---

## Task 6: Podcast タブ + 音声プレイヤー

**Files:**
- Create: `ios/NewsListenApp/NewsListenApp/Podcast/PodcastViewModel.swift`
- Create: `ios/NewsListenApp/NewsListenApp/Podcast/AudioPlayerView.swift`
- Create: `ios/NewsListenApp/NewsListenApp/Podcast/PodcastRowView.swift`
- Create: `ios/NewsListenApp/NewsListenApp/Podcast/PodcastView.swift`
- Create: `ios/NewsListenApp/NewsListenAppTests/PodcastViewModelTests.swift`

- [x] **Step 1: PodcastViewModel のテストを書く（RED）**

```swift
// ios/NewsListenApp/NewsListenAppTests/PodcastViewModelTests.swift
import XCTest
@testable import NewsListenApp

@MainActor
final class PodcastViewModelTests: XCTestCase {

    func testLoadPodcastsPopulatesList() async throws {
        let mockJSON = """
        {"podcasts": [
            {"id":"p1","type":"single","article_ids":["a1"],"difficulty":"toeic_900","audio_url":"https://storage.example.com/p1.mp3","japanese_intro_text":"今日は...","duration_seconds":300,"created_at":"2026-05-31T06:00:00Z"}
        ]}
        """.data(using: .utf8)!

        let client = APIClient(
            baseURL: URL(string: "https://api.example.com")!,
            apiKey: "key",
            session: MockURLSession(data: mockJSON, statusCode: 200)
        )
        let vm = PodcastViewModel(apiClient: client)

        await vm.loadPodcasts()

        XCTAssertEqual(vm.podcasts.count, 1)
        XCTAssertEqual(vm.podcasts[0].id, "p1")
        XCTAssertFalse(vm.isLoading)
    }
}
```

- [x] **Step 2: PodcastViewModel.swift を作成（GREEN）**

```swift
// ios/NewsListenApp/NewsListenApp/Podcast/PodcastViewModel.swift
import Foundation
import AVFoundation

@MainActor
final class PodcastViewModel: NSObject, ObservableObject {
    @Published var podcasts: [Podcast] = []
    @Published var isLoading = false
    @Published var errorMessage: String?
    @Published var currentPodcast: Podcast?
    @Published var isPlaying = false
    @Published var currentTime: Double = 0
    @Published var duration: Double = 0
    @Published var playbackSpeed: Float = 1.0

    private let apiClient: APIClient
    private var player: AVPlayer?
    private var timeObserver: Any?

    init(apiClient: APIClient) {
        self.apiClient = apiClient
    }

    // MARK: - Data

    func loadPodcasts() async {
        isLoading = true
        errorMessage = nil
        do {
            let response = try await apiClient.fetchPodcasts()
            podcasts = response.podcasts
        } catch {
            errorMessage = error.localizedDescription
        }
        isLoading = false
    }

    // MARK: - Playback

    func play(podcast: Podcast) {
        guard let url = URL(string: podcast.audioUrl) else { return }

        stopPlayback()
        currentPodcast = podcast

        let playerItem = AVPlayerItem(url: url)
        player = AVPlayer(playerItem: playerItem)
        player?.rate = playbackSpeed

        // 再生位置の定期更新（0.5秒ごと）
        timeObserver = player?.addPeriodicTimeObserver(
            forInterval: CMTime(seconds: 0.5, preferredTimescale: 600),
            queue: .main
        ) { [weak self] time in
            self?.currentTime = time.seconds
            self?.duration = playerItem.duration.seconds.isNaN ? 0 : playerItem.duration.seconds
        }

        player?.play()
        isPlaying = true
    }

    func togglePlayPause() {
        guard let player else { return }
        if isPlaying {
            player.pause()
        } else {
            player.play()
        }
        isPlaying.toggle()
    }

    func seek(to seconds: Double) {
        player?.seek(to: CMTime(seconds: seconds, preferredTimescale: 600))
    }

    func setSpeed(_ speed: Float) {
        playbackSpeed = speed
        if isPlaying { player?.rate = speed }
    }

    func stopPlayback() {
        if let obs = timeObserver { player?.removeTimeObserver(obs) }
        player?.pause()
        player = nil
        isPlaying = false
        currentTime = 0
        duration = 0
    }
}
```

- [x] **Step 3: AudioPlayerView.swift を作成**

```swift
// ios/NewsListenApp/NewsListenApp/Podcast/AudioPlayerView.swift
import SwiftUI

struct AudioPlayerView: View {
    @ObservedObject var vm: PodcastViewModel

    private let speeds: [Float] = [0.5, 0.75, 1.0, 1.25, 1.5, 1.75, 2.0, 2.5]

    var body: some View {
        VStack(spacing: 16) {
            // 日本語イントロ表示
            if let podcast = vm.currentPodcast, !podcast.japaneseIntroText.isEmpty {
                Text(podcast.japaneseIntroText)
                    .font(.subheadline)
                    .foregroundStyle(.secondary)
                    .padding(.horizontal)
                    .multilineTextAlignment(.center)
            }

            // シークバー
            VStack(spacing: 4) {
                Slider(
                    value: Binding(
                        get: { vm.currentTime },
                        set: { vm.seek(to: $0) }
                    ),
                    in: 0...max(vm.duration, 1)
                )
                HStack {
                    Text(formatTime(vm.currentTime))
                    Spacer()
                    Text(formatTime(vm.duration))
                }
                .font(.caption)
                .foregroundStyle(.secondary)
            }
            .padding(.horizontal)

            // 再生コントロール
            HStack(spacing: 40) {
                Button {
                    vm.seek(to: max(0, vm.currentTime - 15))
                } label: {
                    Image(systemName: "gobackward.15")
                        .font(.title2)
                }

                Button {
                    vm.togglePlayPause()
                } label: {
                    Image(systemName: vm.isPlaying ? "pause.circle.fill" : "play.circle.fill")
                        .font(.system(size: 52))
                }

                Button {
                    vm.seek(to: min(vm.duration, vm.currentTime + 30))
                } label: {
                    Image(systemName: "goforward.30")
                        .font(.title2)
                }
            }

            // 再生速度
            Picker("速度", selection: Binding(
                get: { vm.playbackSpeed },
                set: { vm.setSpeed($0) }
            )) {
                ForEach(speeds, id: \.self) { speed in
                    Text("×\(speed, specifier: speed == Float(Int(speed)) ? "%.1f" : "%.2f")")
                        .tag(speed)
                }
            }
            .pickerStyle(.segmented)
            .padding(.horizontal)
        }
        .padding(.vertical)
        .background(.regularMaterial)
        .clipShape(RoundedRectangle(cornerRadius: 16))
        .padding()
    }

    private func formatTime(_ seconds: Double) -> String {
        guard seconds.isFinite else { return "0:00" }
        let m = Int(seconds) / 60
        let s = Int(seconds) % 60
        return String(format: "%d:%02d", m, s)
    }
}
```

- [x] **Step 4: PodcastRowView.swift と PodcastView.swift を作成**

```swift
// ios/NewsListenApp/NewsListenApp/Podcast/PodcastRowView.swift
import SwiftUI

struct PodcastRowView: View {
    let podcast: Podcast
    let isPlaying: Bool

    var body: some View {
        HStack {
            Image(systemName: isPlaying ? "waveform" : "headphones")
                .font(.title2)
                .foregroundStyle(isPlaying ? .blue : .secondary)
                .frame(width: 36)

            VStack(alignment: .leading, spacing: 4) {
                Text(podcast.japaneseIntroText.prefix(60))
                    .font(.headline)
                    .lineLimit(2)
                HStack {
                    Text(difficultyLabel(podcast.difficulty))
                        .font(.caption)
                        .padding(.horizontal, 6)
                        .padding(.vertical, 2)
                        .background(Color.blue.opacity(0.1))
                        .clipShape(Capsule())
                    Text(podcast.formattedDuration)
                        .font(.caption)
                        .foregroundStyle(.secondary)
                    Spacer()
                    Text(podcast.createdAt.prefix(10))
                        .font(.caption)
                        .foregroundStyle(.secondary)
                }
            }
        }
        .padding(.vertical, 4)
    }

    private func difficultyLabel(_ difficulty: String) -> String {
        switch difficulty {
        case "toeic_600": return "TOEIC 600-"
        case "toeic_900": return "TOEIC 730-900"
        case "ielts_55": return "IELTS 5.5-6.5"
        case "ielts_7": return "IELTS 7.0+"
        case "eiken_2": return "英検2級"
        case "eiken_p1": return "英検準1級"
        default: return difficulty
        }
    }
}
```

```swift
// ios/NewsListenApp/NewsListenApp/Podcast/PodcastView.swift
import SwiftUI

struct PodcastView: View {
    @EnvironmentObject var appState: AppState
    @StateObject private var viewModel: PodcastViewModel

    init() {
        _viewModel = StateObject(wrappedValue: PodcastViewModel(
            apiClient: APIClient(baseURL: URL(string: "https://placeholder.example.com")!, apiKey: "")
        ))
    }

    var body: some View {
        NavigationStack {
            VStack(spacing: 0) {
                podcastList
                if viewModel.currentPodcast != nil {
                    AudioPlayerView(vm: viewModel)
                        .transition(.move(edge: .bottom))
                }
            }
            .navigationTitle("Podcast")
            .animation(.spring(), value: viewModel.currentPodcast?.id)
        }
        .task {
            guard let client = appState.apiClient else { return }
            // NOTE: StateObject では apiClient を後から差し替えられないため、
            // 実際のプロジェクトでは @Environment(AppState.self) + ObservableObject の
            // 組み合わせに変更することを推奨。ここでは簡略化のため直接呼ぶ。
            await viewModel.loadPodcasts()
        }
    }

    private var podcastList: some View {
        List(viewModel.podcasts) { podcast in
            PodcastRowView(
                podcast: podcast,
                isPlaying: viewModel.currentPodcast?.id == podcast.id && viewModel.isPlaying
            )
            .onTapGesture {
                viewModel.play(podcast: podcast)
            }
        }
    }
}
```

- [x] **Step 5: テストを実行してパスを確認**

Xcode でテストを実行（⌘U）。`PodcastViewModelTests` が通ることを確認。

- [x] **Step 6: コミット**

```bash
git add ios/NewsListenApp/NewsListenApp/Podcast/ ios/NewsListenApp/NewsListenAppTests/PodcastViewModelTests.swift
git commit -m "Podcastタブ（一覧・AVPlayerによる音声再生・速度調整）を実装"
```

---

## Task 7: Settings タブ

> **実施メモ:** Task 5 で `Settings/SettingsView.swift` に「記事の開き方」`Picker`（`AppState.articleOpenMode` バインド）を最小実装済み。Task 7 では難易度・再生速度・RSS ソース管理・API 設定・テーマ切替を追加し、記事の開き方セクションは既存実装を流用する。

**Files:**
- Create: `ios/NewsListenApp/NewsListenApp/Settings/SettingsViewModel.swift`
- Create: `ios/NewsListenApp/NewsListenApp/Settings/SettingsView.swift`
- Create: `ios/NewsListenApp/NewsListenAppTests/SettingsViewModelTests.swift`

- [x] **Step 1: SettingsViewModel のテストを書く（RED）**

```swift
// ios/NewsListenApp/NewsListenAppTests/SettingsViewModelTests.swift
import XCTest
@testable import NewsListenApp

@MainActor
final class SettingsViewModelTests: XCTestCase {

    func testLoadSourcesFetchesFromAPI() async throws {
        let mockJSON = """
        {"sources": [
            {"name":"HackerNews","url":"https://hnrss.org/frontpage"},
            {"name":"Zenn","url":"https://zenn.dev/feed"}
        ]}
        """.data(using: .utf8)!

        let client = APIClient(
            baseURL: URL(string: "https://api.example.com")!,
            apiKey: "key",
            session: MockURLSession(data: mockJSON, statusCode: 200)
        )
        let vm = SettingsViewModel(apiClient: client)

        await vm.loadSources()

        XCTAssertEqual(vm.sources.count, 2)
        XCTAssertEqual(vm.sources[0].name, "HackerNews")
    }

    func testAddSourceAppendsToList() async throws {
        let initialJSON = """{"sources": []}""".data(using: .utf8)!
        let afterAddJSON = """{"sources": [{"name":"TechCrunch","url":"https://techcrunch.com/feed/"}]}""".data(using: .utf8)!

        // 1回目（load）→空、2回目（add）→追加後のリスト
        var callCount = 0
        class SequentialSession: URLSessionProtocol {
            var responses: [Data]
            init(_ responses: [Data]) { self.responses = responses }
            func data(for request: URLRequest) async throws -> (Data, URLResponse) {
                let data = responses.isEmpty ? Data() : responses.removeFirst()
                let response = HTTPURLResponse(url: request.url!, statusCode: 200, httpVersion: nil, headerFields: nil)!
                return (data, response)
            }
        }

        let client = APIClient(
            baseURL: URL(string: "https://api.example.com")!,
            apiKey: "key",
            session: SequentialSession([initialJSON, afterAddJSON])
        )
        let vm = SettingsViewModel(apiClient: client)
        await vm.loadSources()
        await vm.addSource(name: "TechCrunch", url: "https://techcrunch.com/feed/")

        XCTAssertEqual(vm.sources.count, 1)
        XCTAssertEqual(vm.sources[0].name, "TechCrunch")
    }
}
```

- [x] **Step 2: SettingsViewModel.swift を作成（GREEN）**

```swift
// ios/NewsListenApp/NewsListenApp/Settings/SettingsViewModel.swift
import Foundation

@MainActor
final class SettingsViewModel: ObservableObject {
    @Published var sources: [RssSource] = []
    @Published var isLoading = false
    @Published var errorMessage: String?

    private let apiClient: APIClient

    init(apiClient: APIClient) {
        self.apiClient = apiClient
    }

    func loadSources() async {
        isLoading = true
        do {
            let response = try await apiClient.fetchSources()
            sources = response.sources
        } catch {
            errorMessage = error.localizedDescription
        }
        isLoading = false
    }

    func addSource(name: String, url: String) async {
        do {
            let response = try await apiClient.addSource(name: name, url: url)
            sources = response.sources
        } catch {
            errorMessage = error.localizedDescription
        }
    }

    func removeSource(url: String) async {
        do {
            try await apiClient.removeSource(url: url)
            sources.removeAll { $0.url == url }
        } catch {
            errorMessage = error.localizedDescription
        }
    }
}
```

- [x] **Step 3: SettingsView.swift を作成**

```swift
// ios/NewsListenApp/NewsListenApp/Settings/SettingsView.swift
import SwiftUI

struct SettingsView: View {
    @EnvironmentObject var appState: AppState
    @StateObject private var viewModel: SettingsViewModel
    @State private var showAddSource = false
    @State private var newSourceName = ""
    @State private var newSourceURL = ""

    private let difficulties = [
        ("toeic_600", "TOEIC 600以下"),
        ("toeic_900", "TOEIC 730〜900"),
        ("ielts_55", "IELTS 5.5〜6.5"),
        ("ielts_7", "IELTS 7.0以上"),
        ("eiken_2", "英検2級"),
        ("eiken_p1", "英検準1級以上"),
    ]

    init() {
        _viewModel = StateObject(wrappedValue: SettingsViewModel(
            apiClient: APIClient(baseURL: URL(string: "https://placeholder.example.com")!, apiKey: "")
        ))
    }

    var body: some View {
        NavigationStack {
            Form {
                rssSourcesSection
                difficultySection
                apiConfigSection
            }
            .navigationTitle("設定")
            .sheet(isPresented: $showAddSource) { addSourceSheet }
        }
        .task { await viewModel.loadSources() }
    }

    private var rssSourcesSection: some View {
        Section("RSS ソース") {
            ForEach(viewModel.sources) { source in
                VStack(alignment: .leading) {
                    Text(source.name).font(.headline)
                    Text(source.url).font(.caption).foregroundStyle(.secondary)
                }
            }
            .onDelete { indexSet in
                for i in indexSet {
                    let url = viewModel.sources[i].url
                    Task { await viewModel.removeSource(url: url) }
                }
            }
            Button("ソースを追加") { showAddSource = true }
        }
    }

    private var difficultySection: some View {
        Section("デフォルト難易度") {
            Picker("難易度", selection: $appState.defaultDifficulty) {
                ForEach(difficulties, id: \.0) { value, label in
                    Text(label).tag(value)
                }
            }
        }
    }

    private var apiConfigSection: some View {
        Section("API 設定") {
            TextField("API URL", text: $appState.apiBaseURL)
                .autocorrectionDisabled()
                .textInputAutocapitalization(.never)
            SecureField("API キー", text: $appState.apiKey)
        }
    }

    private var addSourceSheet: some View {
        NavigationStack {
            Form {
                TextField("名前 (例: TechCrunch)", text: $newSourceName)
                TextField("RSS URL", text: $newSourceURL)
                    .autocorrectionDisabled()
                    .textInputAutocapitalization(.never)
            }
            .navigationTitle("RSS ソースを追加")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("キャンセル") { showAddSource = false }
                }
                ToolbarItem(placement: .confirmationAction) {
                    Button("追加") {
                        Task {
                            await viewModel.addSource(name: newSourceName, url: newSourceURL)
                            newSourceName = ""
                            newSourceURL = ""
                            showAddSource = false
                        }
                    }
                    .disabled(newSourceName.isEmpty || newSourceURL.isEmpty)
                }
            }
        }
    }
}
```

- [x] **Step 4: テストを実行してパスを確認**

Xcode でテストを実行（⌘U）。全テストが通ることを確認。

- [x] **Step 5: コミット**

```bash
git add ios/NewsListenApp/NewsListenApp/Settings/ ios/NewsListenApp/NewsListenAppTests/SettingsViewModelTests.swift
git commit -m "Settingsタブ（RSSソース管理・デフォルト難易度設定）を実装"
```

---

## Task 8: 実機テスト

- [ ] **Step 1: シミュレーターでビルドして起動**

Xcode で iPhone 17 シミュレーターを選択し ⌘R でビルド・起動。

Expected: 初期設定画面が表示される

- [ ] **Step 2: バックエンドに接続**

初期設定画面で:
- API URL: `https://podcast-api-xxxx-an.a.run.app`（Task 13 Step 5 で確認した URL）
- API キー: Secret Manager に保存した値

Expected: 設定保存後、タブビュー（フィード / Podcast / 設定）が表示される

- [ ] **Step 3: フィードの動作確認**

1. フィードタブを開き記事一覧が表示されることを確認
2. 記事を右スワイプして Star → 一覧から消えることを確認
3. 別の記事を左スワイプして Dismiss → 一覧から消えることを確認

- [ ] **Step 4: Podcast の動作確認**

（バックエンドのバッチが1回以上実行済みの前提）
1. Podcast タブを開き一覧が表示されることを確認
2. エピソードをタップして再生が始まることを確認
3. 速度スライダーで再生速度が変わることを確認
4. シークバーで再生位置を変更できることを確認

- [ ] **Step 5: 全 XCTest を実行（最終確認）**

```
Xcode: ⌘U
```

Expected: 全テストが passed（15件前後）
